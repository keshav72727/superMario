var mario;
var bgImage;
var gameState = 0;
var database;
var form,player,game;

function preload(){
bgImage = loadImage("images/background.jpg");

}

function setup() {

  createCanvas(1000, 600);

  
}

function draw() {
background(bgImage);
}
